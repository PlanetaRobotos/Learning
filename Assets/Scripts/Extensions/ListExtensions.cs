using System;
using System.Collections.Generic;

namespace Extensions
{
    public static class ListExtensions
    {
        // public static bool ContainsKey(this List<Dependency> list, Type type) => 
            // list.Contains(e => e.Type == type);
    }
}